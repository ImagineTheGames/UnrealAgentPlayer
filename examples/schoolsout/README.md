# School's Out VR — test helper examples

Drop `USchoolsOutTestHelpers.h/.cpp` into `Source/SchoolsOut/` in the consuming project and add the source files to the module's build. They auto-register with UnrealAgentPlayer on next editor reload.
